{
  "name": "blog",
  "private": true,
  "scripts": {
    "build": "gridsome build",
    "dev": "gridsome develop",
    "predeploy": "npm run build",
    "deploy": "gh-pages -b blog -d dist",
    "explore": "gridsome explore"
  },
  "dependencies": {
    "@gridsome/source-filesystem": "^0.6.2",
    "axios": "^0.20.0",
    "gh-pages": "^3.1.0",
    "gridsome": "^0.7.0"
  }
}
